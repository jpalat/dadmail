# Server Configuration
PORT=8080
ENVIRONMENT=development
ALLOW_ORIGINS=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=dadmail
DB_PASSWORD=changeme_db_password
DB_NAME=dadmail
DB_SSLMODE=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
# Generate with: openssl rand -base64 32
JWT_SECRET=changeme_jwt_secret_min_32_chars
JWT_ACCESS_TTL=15
JWT_REFRESH_TTL=168

# Email Service Configuration
# Gmail API credentials (get from Google Cloud Console)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=

# Email encryption key (MUST be exactly 32 characters for AES-256)
# Generate with: openssl rand -hex 16
EMAIL_ENCRYPTION_KEY=changeme_32_char_encryption_key

# Object Storage (MinIO/S3)
S3_ENDPOINT=localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=dadmail-attachments
S3_USE_SSL=false
